---
title: "Traj-VLN: Learning Pixel-Space Interaction via Autoregressive Trajectory Generation"
title_zh: Traj-VLN：通过自回归轨迹生成学习像素空间交互
authors: "Changfei Fu, Guangcheng Chen, Wenjun Xu, Hong Zhang"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10744v1"
tags: ["query:emb-wm-gen"]
score: 9.0
evidence: VLN中的仿真到现实和跨场景泛化
tldr: 视觉语言导航（VLN）任务要求智能体遵循指令在连续环境中导航，现有方法将视觉嵌入投射到语言空间，但视觉语言模型（VLMs）主要在RGB图像上训练，难以理解涉及深度感知的3D空间交互。本文提出Traj-VLN，通过自回归轨迹生成直接在2D像素空间微调VLMs，根据语言指令和历史观测预测一系列像素坐标，绘制从当前视野底部中心的轨迹。实验表明，像素空间轨迹监督显著优于离散动作学习，旗舰模型在有限计算资源和训练数据下达到最先进水平。该工作展示了利用VLMs自身像素空间能力解决空间交互任务的新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10744v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 889, \"height\": 729, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 632, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 922, \"height\": 477, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 920, \"height\": 505, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10744v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 893, \"height\": 633, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10744v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1845, \"height\": 550, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10744v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1845, \"height\": 385, \"label\": \"Table\"}]"
motivation: VLMs难以处理涉及深度感知的3D空间交互，传统方法引入几何信息不匹配预训练数据，需探索直接在2D像素空间学习导航交互。
method: 微调VLMs通过自回归生成像素坐标轨迹，以语言指令和历史观测为条件，预测从当前观测底部中心开始的连续轨迹。
result: 像素空间轨迹监督显著提升VLN性能，旗舰模型在受限资源下达到SOTA水平，优于离散动作学习和像素目标监督。
conclusion: 证明了无需3D几何信息，仅利用2D像素空间轨迹即可有效学习VLN中的空间交互，为VLMs应用于具身导航提供新思路。
---

## 摘要
受益于大规模预训练数据中嵌入的强大先验知识和新兴的常识推理能力，大语言模型（LLM）在许多研究领域展现出了前所未有的泛化能力。近年来，通过视觉-语言模型（VLM）将视觉嵌入投射到语言空间，以实现仿真到真实和跨场景泛化，已成为连续环境视觉语言导航（VLN-CE）领域的主流范式。VLN要求具身智能体在遵循自然语言指令的情况下，在未见过的环境中导航。我们强调，VLN任务可以分解为一系列子任务，每个子任务对应一个与环境的3D空间交互过程，例如指令“走到沙发尽头然后左转”。然而，这类涉及沿着深度感知方向进入图像的空间交互对VLM来说令人困惑，因为它们主要基于RGB图像的对话进行训练。我们没有融入深度或3D几何信息（VLM在预训练中很少遇到），而是提出了一种替代方法：通过自回归轨迹生成，直接让VLM在二维像素空间中学习导航交互。给定语言指令和历史观察，我们的模型顺序预测一系列像素坐标，在当前观察的底部中心绘制一条轨迹。虽然先前的工作已经证明像素目标监督优于离散动作学习，但我们的实验进一步验证了像素空间轨迹的监督显著提升了VLN性能。此外，我们展示出我们的旗舰模型在相对有限的计算资源和训练数据下达到了当前最优性能水平。

## Abstract
Benefiting from the powerful priors embedded in large-scale pre-training data and the emerging commonsense reasoning ability, large language models (LLMs) have shown unprecedented generalization capabilities in many research fields. Recently, projecting visual embeddings into the language space via vision-language models (VLMs) to achieve sim-toreal and cross-scene generalization has become a prevailing paradigm in the field of Vision-and-Language Navigation in Continuous Environments (VLN-CE). VLN requires an embodied agent to navigate through unseen environments following natural linguistic instructions. We emphasize that a VLN task can be decomposed into a sequence of sub-tasks, each corresponding to a process of 3D spatial interaction with the environments described by instructions such as "walk to the end of the sofa and turn left." However, such spatial interactions involving moving into the image along the direction of depth sensing are puzzling for VLMs as they were predominantly trained on conversations with RGB images. Rather than incorporating depth or 3D geometric information-which VLMs rarely encounter during pretrainingwe propose an alternative approach: fine-tuning VLMs to learn navigation interactions directly in 2D pixel space through autoregressive trajectory generation. Given a linguistic instruction and historical observations, our model sequentially predicts a series of pixel coordinates, drawing a trajectory from the bottom center of the current observation. While prior work has proved that pixel-goal supervision outperforms learning of discrete actions, our experiments further verify that the supervision of pixel-space trajectory significantly enhances VLN performance. Moreover, we demonstrate that our flagship model achieves state-of-the-art level performance with relatively limited computational resources and training data.

---

## 论文详细总结（自动生成）

# 论文详细总结：Traj-VLN

## 1. 核心问题与整体含义（研究动机与背景）

- **问题背景**：视觉语言导航（VLN）在连续环境（VLN-CE）中要求智能体遵循自然语言指令，在未见过场景中导航。传统方法通过微调视觉语言模型（VLM），将视觉信息投射到语言空间，但VLM预训练数据以RGB图像对话为主，缺乏对深度感知和3D空间交互的理解。引入额外的深度或3D几何信息会与预训练分布不匹配，且需要额外对齐模块。
- **研究动机**：提出直接在2D像素空间中学习导航交互，避免VLM不擅长的3D推理。将VLN任务分解为一系列子任务（如“走到沙发尽头然后左转”），每个子任务对应一次3D空间交互。作者认为，通过自回归生成像素轨迹（从观测底部中心开始）可让模型学习交互过程，从而更好地预测目标像素。
- **整体含义**：证明了仅依赖2D像素空间的轨迹监督，无需显式3D几何信息，即可显著提升VLM在VLN任务上的性能，为VLM在具身导航中的应用提供了新范式。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：微调通用VLM（Qwen3-VL-8B）使其在2D像素空间中自回归生成轨迹，将导航交互过程建模为在观测图像上绘制从近到远的像素坐标序列。
- **关键技术细节**：
  - **两阶段对话框架**：
    - 第一阶段：VLM根据当前和历史RGB观测，输出转向动作（←、→、↓）或“STOP”。若输出“↓”，则进入第二阶段。
    - 第二阶段：VLM接收当前位姿下的俯视图（通过降低摄像头30度获取），并自回归生成一系列归一化像素坐标 `[u1,v1] [u2,v2] ... [uk,vk]`，在俯视图上从底部中心开始描绘轨迹。
  - **执行阶段**：根据俯视图对应的深度图，将像素坐标转换为3D坐标，并控制机器人依次移动至每个3D路点，直到当前规划完成，然后重启对话。
  - **训练数据生成**：从标准数据集（R2R-CE、RxR-CE、ScaleVLN）中，以固定间隔（4步）采样。将未来机器人位姿投影到当前俯视图，过滤掉被遮挡或重复的像素点，得到地面真值轨迹作为监督。最终构建1.75M个样本，每个样本包含指令、历史图像、当前图像和对应的监督信号（转向动作、STOP或像素坐标序列）。
  - **公式（投影变换）**：使用相机内参矩阵K和机器人基座坐标系变换T₍c2b₎，将3D点(Xᵢ,Yᵢ,Zᵢ)投影到像素坐标(uᵢ,vᵢ)：  
    `(Xᵢ,Yᵢ,Zᵢ,1)ᵀ = dᵢ · T₍c2b₎⁻¹ · K⁻¹ · (uᵢ,vᵢ,1,1/dᵢ)ᵀ`
  - **模型微调**：冻结视觉编码器，仅微调LLM和投影层（全量微调），学习率2e-5，单epoch。对比实验中使用LoRA（rank=32）微调。

## 3. 实验设计

- **数据集与场景**：使用R2R-CE（约10.7K轨迹，152K样本）、RxR-CE（约19.5K轨迹，455.6K样本）以及ScaleVLN子集（约77.3K轨迹，1.14M样本），共约107.5K轨迹，1.75M训练样本。
- **基准测试**：
  - R2R-CE Val-Unseen：1,839条轨迹
  - RxR-CE Val-Unseen：3,669条轨迹
- **评估指标**：导航误差（NE）、Oracle成功率（OS）、成功率（SR）、成功加权路径长度（SPL）、标准化动态时间规整（nDTW）。
- **对比方法**：
  - VLM方法：StreamVLN、NaVid、NaVid-4D、NaVILA、JanusVLN、InternVLA-N1、Goal2Pixel
  - 传统方法：Sim2Real、AO-Planner、NavMorph
- **主要实验**：
  - Table I：旗舰模型与SOTA方法在两大基准上的全面对比。
  - Table II & Figure 4：在相同配置下，仅改变监督信号（像素目标 vs. 像素轨迹），比较Traj-VLN与像素目标监督（InternVLA-N1-S2），并研究不同执行步数的影响。
  - Traj-CoT验证：将Traj-VLN预测轨迹的最后一个像素作为像素目标进行评估，验证轨迹生成是否起到链式思考（CoT）作用。

## 4. 资源与算力

- **旗舰模型（Qwen3-VL-8B）**：
  - 硬件：8张Ascend 910C NPU
  - 训练时长：约91小时（总计728 NPU小时）
  - 批次大小：128，步数：16,998步，单epoch
- **对比实验（Qwen2.5-VL-7B，LoRA）**：
  - 硬件：4张Nvidia 4090 GPU（每张48GB）
  - 训练时长：约171小时，步数：274,266步
- **资源节省策略**：冻结视觉编码器，相比全量微调节省约62.5%的训练时间。

## 5. 实验数量与充分性

- **实验数量**：在两个主要基准上报告了完整指标，并在Table II中进行了与像素目标监督的严格对比（包括不同执行步数的消融），以及Traj-CoT验证实验。此外，Figure 4展示了不同执行步数下的SR/OS趋势。
- **充分性与公平性**：
  - ✓ 控制变量实验：在相同骨干网络、数据、训练设置下仅改变监督信号，对比公平。
  - ✓ 旗舰模型与SOTA对比时，明确标注了训练数据量差异（1.75M vs 4.72M等），结论可信。
  - △ 但未包含真实世界实验（仅定性提及在杂乱环境中的交互能力），也未测试其他VLM架构（如LLaVA、VILA）的迁移性。
  - △ 消融实验种类相对有限（未对历史图像数量、投影策略等进行详细分析），但核心对比（轨迹 vs 目标）扎实。

## 6. 主要结论与发现

1. **性能优势**：旗舰模型Traj-VLN在R2R-CE Val-Unseen上达到SR 58.4%，在RxR-CE Val-Unseen上达到SR 52.6%，超越所有使用相当规模训练数据的SOTA方法（如InternVLA-N1、Goal2Pixel），甚至优于部分引入3D信息的方法（如JanusVLN）。
2. **轨迹监督优于像素目标监督**：在严格公平对比下（相同骨干、数据、训练配置），Traj-VLN（像素轨迹）相比像素目标监督在R2R上SR提升4.0%（43.2% vs 39.6%），在RxR上提升3.29%（41.54% vs 36.09%）。
3. **轨迹生成作为CoT机制**：将预测轨迹的最后一个像素作为像素目标进行评估（Final-Pixel Evaluation），性能仍显著优于直接像素目标监督，证明自回归生成过程有效分解了最终目标，起到链式思考作用。
4. **无需3D几何信息**：仅在2D像素空间学习交互即可获得高性能，验证了VLM可通过微调直接学习像素空间的交互模式，弥补了其在3D感知上的不足。

## 7. 优点

- **方法创新性**：首次将VLN任务转化为2D像素空间的自回归轨迹生成，避免了VLM不擅长的显式3D推理，且与预训练数据分布自然匹配。
- **效率优势**：冻结视觉编码器大幅节省计算资源；在仅1.75M训练样本和小算力（728 NPU小时）下达到SOTA水平，相比其他方法（如InternVLA-N1需4.72M样本，NaVILA需15.6M样本）更为高效。
- **实验严谨性**：通过控制变量实验直接证明轨迹监督优于像素目标监督，并设计Traj-CoT验证实验揭示机制，增强了方法可信度。
- **实际部署友好**：仅需深度图进行像素到3D的转换，无需复杂的3D对齐模块，降低系统复杂性。

## 8. 不足与局限

- **对深度传感器的依赖**：执行阶段需深度图将像素坐标转换为3D位姿，在深度传感器不准确或缺席的环境中可能受限。
- **场景多样性有限**：训练和测试均基于Matterport3D的室内场景，未验证在开放室外、光照变化、动态障碍等场景下的泛化性。
- **未对比其他VLM骨干**：仅使用Qwen系列模型，未测试在LLaVA、VILA等其他架构上的表现，可能忽略了模型特定行为的影响。
- **部分下游指标略弱**：在RxR数据集上，Traj-VLN的nDTW（62.8）低于InternVLA-N1（62.6差距不大）和Goal2Pixel（63.0），可能与执行多步轨迹导致路径更长有关（见Figure 4，执行步数少时SR高但OS偏低）。
- **消融实验不够全面**：未对不同历史图像数量、不同采样间隔、不同段数设计等进行消融，也未分析遮挡、重复点过滤策略的影响。
- **真实世界部署未验证**：论文仅提供定性交互示例，缺少真实机器人实验数据，实际应用中的传感器噪声、控制误差等挑战未涉及。

（完）
